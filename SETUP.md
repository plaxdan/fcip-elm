Ensure git and NodeJS are on your path, then run the following commands:

```
npm i -g reveal-md
git clone https://github.com/plaxdan/fcip-elm
cd fcip-elm
reveal-md README.md --theme solarized --highlightTheme github
```
